import React from 'react';

const Modal = ({ title, children, isOpen, onAction, onClone }) => {

    if (!isOpen) null;

    return (
        <div>
            { }
        </div>
    );
}

export default Modal;
